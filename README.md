## LangGraph quickstart (Python)

Installation (from LangGraph docs):

```bash
pip install -U langgraph langchain langchain-anthropic
```

Set your Anthropic API key:

```bash
setx ANTHROPIC_API_KEY "your_key_here"
```

Run the example:

```bash
python langgraph_quickstart.py
```
